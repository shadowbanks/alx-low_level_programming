#include <stdio.h>

/**
 * main - Print alphabet 0-9
 *
 *
 * Return: return 0
 */

int main(void)
{
	int count = 0;

	while (count < 10)
	{
		printf("%d", count);
		count++;
	}
	printf("\n");
	return (0);
}
